#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

# Aliases
source $HOME/.aliases

# rbenv
eval "$(rbenv init - bash)"

# Starship
eval "$(starship init bash)"

